class Solution:
    def findKthLargest(self, nums, k: int) -> int:
        # max heap
        nums = [-i for i in nums]
        heapq.heapify(nums)
        while k:
            k -=1 
            ele = heapq.heappop(nums)
        return -ele
