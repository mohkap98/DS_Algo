class Solution:
    def multiply(self, num1: str, num2: str) -> str:
        n1, n2 = 0,0
        for d in num1:
            n1 = n1 * 10 + (ord(d) - 48)
        for d in num2:
            n2 = n2 * 10 + (ord(d) - 48)
        
        return str(n1 * n2)
        
